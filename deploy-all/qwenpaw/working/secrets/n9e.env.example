# 夜莺(Nightingale/N9E)日志查询共享凭证（统一管理）
#
# 复制本文件为同目录下的 n9e.env 并填入真实值。n9e.env 不入库（见 .gitignore）。
# 解析优先级（同名 key，高 → 低）：
#   1. 真实进程环境变量 / export / 后端 qwenpaw env 存储
#   2. 本共享文件 secrets/n9e.env（新逻辑，统一来源）
#   3. 各 skill 目录下的 .env（旧配置，兼容；独立运行 skill 时作为可选覆盖）
#
# 仅放公共连接配置；N9E_LOG_MAX_SIZE / N9E_LOG_TIMEOUT 各 skill 取值不同，
# 请保留在各自 skill 的 .env 里，不要收进本文件。

N9E_API_BASE_URL=http://<host>:<port>
N9E_USER_TOKEN=your_n9e_user_token_here
N9E_LOG_DATASOURCE_ID=1
N9E_LOG_INDEX=your-log-index-*
N9E_LOG_TIMESTAMP_FIELD=@timestamp
