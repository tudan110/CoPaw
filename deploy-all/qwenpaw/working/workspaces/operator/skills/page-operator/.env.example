# page-operator 配置示例
#
# 本技能默认只用本地操作目录(catalog/operations.json)做匹配 + 出指令,
# 不需要任何网络配置即可工作。
#
# 仅当用 emit_action.py --resolve-route(运行期按 component 反查线上真实路由)
# 或用 scan_catalog.py 想顺带回填 route 时,才需要门户菜单接口。
#
# 推荐在门户「设置 - 操作」分类里配置(会物化为下面的 OPERATOR_MENU_* 环境
# 变量);也可直接 export。解析优先级:OPERATOR_MENU_* > INOE_MENU_* >
# INOE_API_*(与 page-navigator / 告警同一 INOE 后端,可来自 secrets/inoe.env)。
#
# operator 专属(留空则回退共享菜单 / INOE 接入变量):
# OPERATOR_MENU_BASE_URL=
# OPERATOR_MENU_TOKEN=
# OPERATOR_MENU_APP_CODE=inoe
# OPERATOR_MENU_TIMEOUT_SECONDS=20
# OPERATOR_MENU_CACHE_TTL_SECONDS=600
#
# 共享回退(page-navigator / 告警同后端时复用):
# INOE_API_BASE_URL=https://your-inoe-host
# INOE_API_TOKEN=your-token
# INOE_MENU_BASE_URL=
# INOE_MENU_TOKEN=
# INOE_MENU_APP_CODE=inoe
# INOE_MENU_TIMEOUT_SECONDS=20
# INOE_MENU_CACHE_TTL_SECONDS=600
